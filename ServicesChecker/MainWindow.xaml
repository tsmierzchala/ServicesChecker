<Window x:Class="ServicesChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServicesChecker"
        mc:Ignorable="d"
        Title="Services Checker" 
        Width="350"
        MinHeight="200"
        MaxWidth="350"
        MinWidth="350"
        SizeToContent="Height"
        >

    <Window.Resources>
        <local:UrlShortenerConverter x:Key="UrlShortenerConverter"/>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="Services">
                <StackPanel>
                    <TextBox x:Name="ServiceNameTextBox" Width="200" Margin="10,10,10,0"/>
                    <CheckBox x:Name="IsConnectingToDBCheckBox" Content="Is Connecting to DB" Margin="100,0,100,0"/>
                    <Button x:Name="AddServiceButton" Content="Add Service" Click="AddServiceButton_Click" Width="200" Margin="10"/>
                    <Button x:Name="GetAppsButton" Content="Get Apps" Click="GetAppsButton_Click" Height="30" Margin="10"/>
                    <ProgressBar x:Name="LoadingProgressBar" 
                     IsIndeterminate="True" 
                     Visibility="Collapsed" 
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     Width="328" Height="30" Margin="10"/>
                    <TextBlock x:Name="LoadingStatusTextBlock" 
                       Visibility="Collapsed" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Width="200" Height="30" Margin="0,-35,0,0" TextAlignment="Center"/>
                    <ComboBox x:Name="DockerContainerComboBox" SelectionChanged="DockerContainerComboBox_SelectionChanged" Width="190" Height="30" VerticalAlignment="Center" Margin="119,0,0,0" Panel.ZIndex="1">
                        <!-- Items will be added dynamically -->
                    </ComboBox>
                    <Label Content="Change container" Margin="10,-29,0,0" FontWeight="Bold"/>
                    <ListView x:Name="ServiceStatusListView"
                              ContextMenuOpening="ServiceStatusListView_ContextMenuOpening"
                              Margin="10" 
                              ItemsSource="{Binding ServiceStatuses}">
                        <ListView.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Delete" Click="DeleteMenuItem_Click"/>
                                <MenuItem Header="Change status" Click="ChangeServiceStatusMenuItem_Click"/>
                            </ContextMenu>
                        </ListView.ContextMenu>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Service Name" DisplayMemberBinding="{Binding Name, Converter={StaticResource UrlShortenerConverter}}" Width="200"/>
                                <GridViewColumn Header="Status" Width="95">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Status}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </TabItem>
            <TabItem Header="Configuration">
                <Grid>
                    <StackPanel Margin="10">
                        <TextBlock Text="Log Files Management" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <!-- Log file management buttons -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button x:Name="BrowseLogFileButton" Content="Browse" Width="150" Click="BrowseLogFileButton_Click" HorizontalAlignment="Left" Margin="0,0,10,0"/>
                            <Button x:Name="DeleteLogFilesFromDiskButton" Content="Delete From Disk" Width="150" Click="DeleteLogFilesFromDiskButton_Click" HorizontalAlignment="Left"/>
                        </StackPanel>
                        
                        <!-- Log files list -->
                        <ListView x:Name="LogFilesListView" Height="200" Margin="0,0,0,10">
                            <ListView.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Delete" Click="DeleteLogFileMenuItem_Click"/>
                                    <MenuItem Header="Open Location" Click="OpenLogLocationMenuItem_Click"/>
                                </ContextMenu>
                            </ListView.ContextMenu>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="File Name" Width="200">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="10" Height="10" Fill="{Binding StatusIndicator}" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding FileName}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding FileSize}" Width="95"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Statistics">
                <Grid>
                    <TextBlock Text="Statistics tab content" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               FontStyle="Italic" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
